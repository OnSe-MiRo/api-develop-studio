# OpenAPI/Swagger YAML document import.
PyYAML>=6.0,<7

# Generate the Studio FastAPI server and language-specific client SDK archives.
openapi-generator-cli==7.24.0

# Fernet authenticated encryption for project secret variables.
cryptography>=43,<50

# NTLM authentication support for API requests.
requests-ntlm>=1.2,<2

# ASGI API server (single worker while SQLite/subprocess execution is in use).
fastapi==0.137.0
uvicorn==0.49.0

# FastAPI HTTP contract tests.
httpx==0.28.1

# FND-4 permanent storage and disposable revision metadata cache.
psycopg[binary,pool]>=3.2,<4
redis>=5,<7

# OAS-2 OpenAPI 3.0/3.1 structure and response validation.
openapi-spec-validator==0.7.2
openapi-schema-validator==0.6.3
jsonschema>=4.23,<5
