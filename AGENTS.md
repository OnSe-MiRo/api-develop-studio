# 프로젝트 작업 규칙

이 문서는 이 저장소에서 작업하는 자동화 에이전트와 개발자가 따라야 할 기본 규칙이다.

## 변경 작업

- 기존 사용자 변경사항은 임의로 되돌리거나 삭제하지 않는다.
- 요청과 직접 관련된 파일만 수정한다.
- 파일 수정 후 관련 테스트를 실행한다. React 화면을 수정했다면 `web/`에서 `npm run build`도 실행한다.
- API 케이스 저장 구조는 `case/{tag}/{api_name}/{case_file}.json`을 유지한다.
- Windows와 macOS/Linux 모두에서 동작하도록 파일 참조 경로는 `/` 구분자를 사용한다.

## Git 브랜치 전략

- `main`은 배포 가능한 안정 버전만 유지하는 기준 브랜치다. 직접 작업하거나 강제 푸시하지 않는다.
- `develop`은 다음 배포를 위한 통합 브랜치다. 일반 기능과 수정은 `develop`에서 분기해 다시 `develop`으로 병합한다.
- 기능 개발은 `feature/{작업-주제}`, 일반 버그 수정은 `fix/{작업-주제}` 브랜치를 사용한다. 작업 주제는 소문자와 하이픈으로 간결하게 작성한다. 예: `feature/openapi-validation`.
- 배포 준비는 `release/{버전}` 브랜치에서 수행한다. 검증이 끝나면 `main`과 `develop`에 모두 병합해 배포 버전과 다음 개발 기준을 일치시킨다.
- 운영 긴급 수정은 `hotfix/{작업-주제}` 브랜치를 최신 `main`에서 생성한다. 수정 후 `main`과 `develop`에 모두 병합한다.
- 하나의 작업 브랜치에는 하나의 독립적인 변경 목적만 담는다. 병합 전에는 대상 브랜치의 최신 변경을 반영하고 관련 검증을 완료한다.
- 브랜치 병합과 삭제는 사용자가 명시적으로 요청한 경우에만 수행한다. 병합 완료 브랜치는 필요 없을 때만 삭제하며, `main`과 `develop`은 삭제하지 않는다.
- 잘못 반영된 변경은 새 `fix/` 또는 `hotfix/` 브랜치에서 수정하거나 `git revert`로 복구한다. 공유 이력에는 `git reset`을 적용하지 않는다.
- 커밋 전에는 `git status --short`, `git diff --check`, 관련 테스트 결과를 확인한다.
- `git add -- <명시한 파일 목록>`만 사용한다. `git add .`, `git add -A`, `git add --all`은 사용하지 않는다.

## 완료 보고

- 사용자에게 수정 내용, 실행한 검증, 최종 반영 브랜치를 간결하게 알린다.
- 원격 저장소에 반영했다면 대상 브랜치와 반영 커밋을 함께 알린다.
