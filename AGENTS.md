# 프로젝트 작업 규칙

이 문서는 이 저장소에서 작업하는 자동화 에이전트와 개발자가 따라야 할 기본 규칙이다.

## 변경 작업

- 기존 사용자 변경사항은 임의로 되돌리거나 삭제하지 않는다.
- 요청과 직접 관련된 파일만 수정한다.
- 파일 수정 후 관련 테스트를 실행한다. React 화면을 수정했다면 `web/`에서 `npm run build`도 실행한다.
- API 케이스 저장 구조는 `case/{tag}/{api_name}/{case_file}.json`을 유지한다.
- Windows와 macOS/Linux 모두에서 동작하도록 파일 참조 경로는 `/` 구분자를 사용한다.

## Git 및 GitHub 반영

- 사용자가 명시적으로 요청하기 전에는 커밋, 푸시, Pull Request 생성 또는 병합을 수행하지 않는다.
- 사용자가 "Git에 올려", "main에 올려", "main 브랜치에 반영"을 요청하면 최종 반영 대상은 항상 `main`이다.
- `main`에서 직접 작업하거나 강제 푸시하지 않는다. 최신 `main`에서 작업 브랜치를 만든 뒤 `main` 대상 Pull Request로 병합한다.
- 커밋 전에는 `git status --short`, `git diff --check`, 관련 테스트 결과를 확인한다.
- `git add -- <명시한 파일 목록>`만 사용한다. `git add .`, `git add -A`, `git add --all`은 사용하지 않는다.
- Pull Request는 base 브랜치를 `main`으로 지정하고, 병합 가능 상태를 확인한 후 병합한다.
- 병합 뒤에는 로컬 `main`을 fast-forward로 동기화하고 반영 커밋을 확인한다.
- 작업 브랜치 삭제는 사용자가 요청했거나 PR 병합 과정에서 명시적으로 선택한 경우에만 수행한다. `main`은 삭제하지 않는다.
- 공유된 `main` 이력은 `git reset` 또는 강제 푸시로 바꾸지 않는다. 잘못된 반영은 새 수정 PR 또는 `git revert`로 복구한다.

## 완료 보고

- 사용자에게 수정 내용, 실행한 검증, 최종 반영 브랜치를 간결하게 알린다.
- GitHub에 반영했다면 PR 링크와 병합 커밋을 함께 알린다.
